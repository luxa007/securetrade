# SecureTrade API
JWT auth, RBAC, CRUD REST API with React frontend.
API Docs: http://localhost:8000/docs
Run backend: cd backend && source venv/bin/activate && uvicorn app.main:app --reload
Run frontend: cd frontend && npm start
